@{
    Layout = null;
    ViewData["Title"] = "Login";
}
<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <style>
        .main-page {
            margin-top: 7.5%
        }

        .image-login {
            width: 80px;
            height: 80px
        }
    </style>
</head>
<body>
    <form id="form-login">
        <div class="container main-page">
            <div class="col-md-4 offset-md-4">
                <div class="card">
                    <div class="card-header text-center">
                        <h2>Login</h2>
                    </div>
                    <div class="card-body">
                        <div class="form-group">
                            <label for="txtEmail">Username</label>
                            <input type="email" class="form-control" id="txtUsername" aria-describedby="userHelp" placeholder="Username" required="">
                            <small id="emailHelp" class="form-text text-muted">Please input correctly.</small>
                        </div>
                        <div class="form-group">
                            <label for="txtPassword">Password</label>
                            <input type="password" class="form-control" id="txtPassword" placeholder="Password">
                        </div>
                    </div>
                    <div class="card-footer">
                        <button type="button" class="btn btn-primary float-left" onclick="onlogin();">Submit</button>
                    </div>
                </div>
            </div>
        </div>
    </form>
    <script src="~/lib/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            
        });
        function onlogin() {
            $.ajax({
                type : "GET",
                url : "/LoginGET",
                data : {
                    username : document.getElementById("txtUsername").value,
                    password : document.getElementById("txtPassword").value
                },
                dataType : "json",
                success : function (e) {
                    if(e.success == true){
                        window.location.href = "/Home"
                    }
                    else{
                        alert(e.message);
                    }
                }
            })
            /*let xmlHttp = new XMLHttpRequest();
            xmlHttp.open("GET", "https://localhost:7253/api/login?username=" + username + "&pass", false); // false for synchronous request
            xmlHttp.send(null);
            console.log(xmlHttp.responseText);
            return xmlHttp.responseText*/
        }
    </script>
</body>